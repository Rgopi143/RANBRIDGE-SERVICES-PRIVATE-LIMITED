
/* Team component removed — kept as a harmless stub in case of accidental imports.
   If you want to restore the original Team component, retrieve it from Git history. */

import * as React from 'react';

const Team = () => {
  return null; // Component intentionally removed
};

export default Team;
